<p>LINK BACK TO INDEX PAGE</p>
<h2>Show page for specific collage</h2>

<% if @collage.giftape.user == current_user %>
  <%= link_to "Delete Collage",
      collage_path(@collage),
      method: :delete %>
<% end %>

<% @collage.collage_items.each do |item| %>
  <p><%= item.prompt %></p>
  <%= cl_image_tag(item.photo.key, transformation: [
      # Adds text on the image hahaha
      { width: 400, height: 450, crop:'fill' },
      { overlay: { font_family: "Cookie", font_size: 40, font_weight: "bold", text: "Love"},
        effect: "colorize",
        color: "#f08" },
      { angle: 20, flags: "layer_apply", x: -45, y: 44 }]) %>
  <%= link_to "Delete",
    collage_collage_item_path(@collage, item),
    method: :delete %>

<% end %>